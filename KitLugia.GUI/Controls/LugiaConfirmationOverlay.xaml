<UserControl x:Class="KitLugia.GUI.Controls.LugiaConfirmationOverlay"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             HorizontalAlignment="Stretch" VerticalAlignment="Stretch">

    <Border Width="500" Height="240" CornerRadius="12" BorderThickness="1" BorderBrush="{StaticResource AccentColor}" Background="#1A1A1A"
            HorizontalAlignment="Center" VerticalAlignment="Center">
        <Border.Effect>
            <DropShadowEffect Color="#FFD700" BlurRadius="30" ShadowDepth="0" Opacity="0.4"/>
        </Border.Effect>

        <Grid Margin="25">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Título -->
            <TextBlock Text="CONFIRMAÇÃO" Foreground="{StaticResource AccentColor}" 
                       FontWeight="Bold" FontSize="16" HorizontalAlignment="Center"/>

            <!-- Mensagem com Auto-Resize (Viewbox) -->
            <StackPanel Grid.Row="1" VerticalAlignment="Center">
                <TextBlock Text="⚠️" FontSize="36" HorizontalAlignment="Center" Margin="0,0,0,10"/>

                <!-- O Viewbox faz o texto diminuir se for muito largo -->
                <Viewbox MaxHeight="60" StretchDirection="DownOnly">
                    <TextBlock x:Name="TxtMessage" 
                               Text="Deseja continuar?" 
                               Foreground="#E0E0E0" 
                               TextAlignment="Center" 
                               FontSize="15" 
                               Margin="10,0"/>
                </Viewbox>
            </StackPanel>

            <!-- Botões -->
            <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,15,0,0">
                <Button Content="SIM" Width="120" Height="40" Margin="0,0,20,0" Click="BtnYes_Click" Cursor="Hand">
                    <Button.Template>
                        <ControlTemplate TargetType="Button">
                            <Border x:Name="bdr" Background="{StaticResource AccentColor}" CornerRadius="20">
                                <TextBlock Text="{TemplateBinding Content}" Foreground="Black" FontWeight="ExtraBold" 
                                           HorizontalAlignment="Center" VerticalAlignment="Center" FontSize="14"/>
                            </Border>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter TargetName="bdr" Property="Background" Value="White"/>
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Button.Template>
                </Button>

                <Button Content="NÃO" Width="120" Height="40" Click="BtnNo_Click" Cursor="Hand">
                    <Button.Template>
                        <ControlTemplate TargetType="Button">
                            <Border x:Name="bdr" Background="#333" CornerRadius="20" BorderBrush="#555" BorderThickness="1">
                                <TextBlock Text="{TemplateBinding Content}" Foreground="White" FontWeight="Bold"
                                           HorizontalAlignment="Center" VerticalAlignment="Center" FontSize="14"/>
                            </Border>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter TargetName="bdr" Property="Background" Value="#444"/>
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Button.Template>
                </Button>
            </StackPanel>
        </Grid>
    </Border>
</UserControl>